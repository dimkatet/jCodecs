{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build:wasm": {
      "cache": false,
      "inputs": ["src/wasm/**", "../../Dockerfile", "../../emscripten-cross.txt"],
      "outputs": ["src/wasm/*.js", "src/wasm/*.wasm"]
    },
    "build:ts": {
      "dependsOn": ["^build", "build:wasm"],
      "outputs": ["dist/**"]
    },
    "build": {
      "dependsOn": ["^build", "build:wasm", "build:ts"],
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "cache": false
    },
    "test:browser": {
      "cache": false
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "clean": {
      "cache": false
    }
  }
}
